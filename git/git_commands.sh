# to list local git profile config
git config list

# to list only the user name or email
git config user.name
git config user.email

# to modify any config
git config --global user.name 'Suresh Siva'
